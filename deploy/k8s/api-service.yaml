apiVersion: v1
kind: Service
metadata:
  name: ai-trace-api
  namespace: ai-trace-prod
  labels:
    app: ai-trace-api
spec:
  selector:
    app: ai-trace-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
