# Frozen Deployment References

Everything under `deploy/` is a frozen manual reference for a future host-owned integration.
This repository does not publish an image, apply these manifests, own a production target, or
run an automatic hosted workload. Applying any artifact requires explicit operator review,
an immutable locally supplied image, host-specific configuration, and separate runtime proof.

Repository files, successful validation, and generated reports are not deployment evidence.
